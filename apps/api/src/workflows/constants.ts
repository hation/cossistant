// Workflow notification constants

// Global message notification delay in minutes (applies to both members and visitors)
// This delay allows grouping multiple messages into a single email batch
export const MESSAGE_NOTIFICATION_DELAY_MINUTES = 1;

// Maximum number of messages to display in email notification
export const MAX_MESSAGES_IN_EMAIL = 3;
