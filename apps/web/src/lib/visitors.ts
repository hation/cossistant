/**
 * Re-export visitor name utilities from the shared package
 * This ensures consistency between frontend and backend
 */
export {
	generateVisitorName,
	getVisitorNameWithFallback,
} from "@cossistant/core";
